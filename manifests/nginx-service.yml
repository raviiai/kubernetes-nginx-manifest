apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  loadBalancerSourceRanges:
  - 152.58.155.153/32
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
